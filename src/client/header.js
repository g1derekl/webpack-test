import React from 'react';
import ReactDOM from 'react-dom';

import '../stylesheets/header.scss';

const Header = React.createClass({
  render() {
    return (
      <div id="header">This is the header bar</div>
    );
  }
});

export default Header;